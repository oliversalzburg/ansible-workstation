---
- name: Create groups
  become: true
  register: _groups
  ansible.builtin.group:
    name: "{{ item }}"
    state: present
    system: true
  loop: "{{ prowlarr_groups }}"

- name: Ensure {{ ansible_user }} is a group member
  become: true
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    groups: "{{ prowlarr_groups }}"
    append: true

- name: Create user
  become: true
  register: _user
  ansible.builtin.user:
    comment: "{{ prowlarr_name }}"
    create_home: false
    group: "{{ _groups.results[0].gid }}"
    groups: "{{ prowlarr_groups }}"
    home: "{{ prowlarr_basedir }}"
    name: "{{ prowlarr_id }}"
    password: "!"
    shell: "/usr/sbin/nologin"
    system: true


- name: Ensure working directory exists
  become: true
  ansible.builtin.file:
    path: "{{ prowlarr_basedir }}"
    state: directory
    owner: "{{ _user.uid }}"
    group: "{{ _groups.results[0].gid }}"

- name: Ensure directories exist
  become: true
  ansible.builtin.file:
    path: "{{ prowlarr_basedir }}/{{ item }}"
    state: directory
    owner: "{{ _user.uid }}"
    group: "{{ _groups.results[0].gid }}"
  loop:
    - config


- name: Create postgres user
  community.postgresql.postgresql_user:
    host: localhost
    login_password: "{{ postgres_master_password }}"
    login_user: "{{ postgres_master_user }}"
    name: "{{ prowlarr_pg_user }}"
    password: "{{ prowlarr_pg_password }}"

- name: Create postgres database
  community.postgresql.postgresql_db:
    host: localhost
    login_password: "{{ postgres_master_password }}"
    login_user: "{{ postgres_master_user }}"
    name: "{{ prowlarr_pg_db }}-{{ item }}"
    owner: "{{ prowlarr_pg_user }}"
  loop:
    - main
    - log

- name: "GRANT ALL PRIVILEGES ON DATABASE 'db' TO 'user'"
  community.postgresql.postgresql_privs:
    host: localhost
    login_password: "{{ postgres_master_password }}"
    login_user: "{{ postgres_master_user }}"
    db: postgres
    privs: ALL
    type: database
    obj: "{{ prowlarr_pg_db }}-{{ item }}"
    role: "{{ prowlarr_pg_user }}"
  loop:
    - main
    - log

- name: "GRANT USAGE, CREATE ON SCHEMA PUBLIC TO 'user'"
  community.postgresql.postgresql_privs:
    host: localhost
    login_password: "{{ postgres_master_password }}"
    login_user: "{{ postgres_master_user }}"
    db: postgres
    privs: CREATE
    type: schema
    obj: public
    role: "{{ prowlarr_pg_user }}"


- name: Copy configuration
  become: true
  ansible.builtin.template:
    src: config.xml.j2
    dest: "{{ prowlarr_basedir }}/config/config.xml"
    owner: "{{ _user.uid }}"
    group: "{{ _groups.results[0].gid }}"
    mode: 0644


- name: Copy service files
  become: true
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "{{ directory_systemd }}/{{ item }}"
    owner: root
    group: root
    mode: 0644
  loop:
    - prowlarr-exporter.service
    - prowlarr-flaresolverr.service
    - prowlarr-main.service
    - prowlarr.service

- name: Enable and check prowlarr service
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
    enabled: true
    name: prowlarr
    state: restarted
