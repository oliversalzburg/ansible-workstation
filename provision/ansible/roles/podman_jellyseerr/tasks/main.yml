---
- name: Create groups
  become: true
  register: _podman_jellyseerr_groups
  ansible.builtin.group:
    name: "{{ item }}"
    state: present
    system: true
  loop: "{{ podman_jellyseerr_groups }}"
  notify: Restart jellyseerr

- name: Ensure user is a group member
  become: true
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    groups: "{{ podman_jellyseerr_groups }}"
    append: true
  notify: Restart jellyseerr

- name: Create user
  become: true
  register: _podman_jellyseerr_user
  ansible.builtin.user:
    comment: "{{ podman_jellyseerr_name }}"
    create_home: false
    group: "{{ _podman_jellyseerr_groups.results[0].gid }}"
    groups: "{{ podman_jellyseerr_groups }}"
    home: "{{ podman_jellyseerr_basedir }}"
    name: "{{ podman_jellyseerr_id }}"
    password: "!"
    shell: "/usr/sbin/nologin"
    system: true
  notify: Restart jellyseerr


- name: Ensure working directory exists
  become: true
  ansible.builtin.file:
    path: "{{ podman_jellyseerr_basedir }}"
    state: directory
    owner: "{{ _podman_jellyseerr_user.uid }}"
    group: "{{ _podman_jellyseerr_groups.results[0].gid }}"
    mode: "u=rwX,g=rX,o="
    recurse: true
  notify: Restart jellyseerr

- name: Ensure directories exist
  become: true
  ansible.builtin.file:
    path: "{{ podman_jellyseerr_basedir }}/{{ item }}"
    state: directory
    owner: "{{ _podman_jellyseerr_user.uid }}"
    group: "{{ _podman_jellyseerr_groups.results[0].gid }}"
    mode: "u=rwX,g=rX,o="
    recurse: true
  loop:
    - config
  notify: Restart jellyseerr


- name: Create postgres user
  register: podman_jellyseerr_result
  retries: 3
  delay: 3
  until: podman_jellyseerr_result is not failed
  community.postgresql.postgresql_user:
    login_host: localhost
    login_password: "{{ podman_postgres_master_password }}"
    login_user: "{{ podman_postgres_master_user }}"
    name: "{{ podman_jellyseerr_pg_user }}"
    password: "{{ podman_jellyseerr_pg_password }}"
  notify: Restart jellyseerr

- name: Create postgres database
  register: podman_jellyseerr_result
  retries: 3
  delay: 3
  until: podman_jellyseerr_result is not failed
  community.postgresql.postgresql_db:
    login_host: localhost
    login_password: "{{ podman_postgres_master_password }}"
    login_user: "{{ podman_postgres_master_user }}"
    name: "{{ podman_jellyseerr_pg_db }}-{{ item }}"
    owner: "{{ podman_jellyseerr_pg_user }}"
  loop:
    - main
    - log
  notify: Restart jellyseerr

- name: "GRANT ALL PRIVILEGES ON DATABASE 'db' TO 'user'"
  community.postgresql.postgresql_privs:
    login_host: localhost
    login_password: "{{ podman_postgres_master_password }}"
    login_user: "{{ podman_postgres_master_user }}"
    login_db: postgres
    privs: ALL
    type: database
    obj: "{{ podman_jellyseerr_pg_db }}-{{ item }}"
    role: "{{ podman_jellyseerr_pg_user }}"
  loop:
    - main
    - log
  notify: Restart jellyseerr

- name: "GRANT USAGE, CREATE ON SCHEMA PUBLIC TO 'user'"
  community.postgresql.postgresql_privs:
    login_host: localhost
    login_password: "{{ podman_postgres_master_password }}"
    login_user: "{{ podman_postgres_master_user }}"
    login_db: postgres
    privs: CREATE
    type: schema
    obj: public
    role: "{{ podman_jellyseerr_pg_user }}"
  notify: Restart jellyseerr


#- name: Copy configuration
#  become: true
#  ansible.builtin.template:
#    src: "config.xml.j2"
#    dest: "{{ podman_jellyseerr_basedir }}/config/config.xml"
#    owner: "{{ _podman_jellyseerr_user.uid }}"
#    group: "{{ _podman_jellyseerr_groups.results[0].gid }}"
#    mode: "0o640"
#  notify: Restart jellyseerr


- name: Copy service files
  become: true
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "{{ directory_systemd }}/{{ item }}"
    owner: root
    group: root
    mode: "0o644"
  loop:
    - jellyseerr-main.service
    - jellyseerr.service
  notify: Restart jellyseerr

- name: Enable and check jellyseerr service
  become: true
  ansible.builtin.systemd:
    enabled: true
    name: jellyseerr
  notify: Restart jellyseerr
