all:
  hosts:
    #canyon.labnet:
    container.labnet:
    #laptop.labnet:
    fireeye.labnet:
    workstation.labnet:
    #sandbox:
    #  ansible_connection: ssh
    #  ansible_host: fitting-toucan.sandbox.rm-rf.link
    #  ansible_user: admin

ca:
  hosts:
    container.labnet:

artifactstores:
  hosts:
    container.labnet:
    fireeye.labnet:

authservers:
  hosts:
    # container.labnet:

gitlabs:
  hosts:
    # workstation.labnet:

gpu_intel:
  hosts:
    container.labnet:

desktops:
  hosts:
    fireeye.labnet:
    workstation.labnet:

hires_desktops:
  hosts:
    workstation.labnet:

servicehosts:
  hosts:
    container.labnet:
    fireeye.labnet:
    workstation.labnet:

dbhosts:
  hosts:
    container.labnet:
    fireeye.labnet:
    workstation.labnet:

dnsresolvers:
  hosts:
    container.labnet:

mediaservers:
  hosts:
    fireeye.labnet:

metricsmonitors:
  hosts:
    fireeye.labnet:
    workstation.labnet:

kittens:
  hosts:
    workstation.labnet:
