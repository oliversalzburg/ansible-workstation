all:
  hosts:
    container.labnet:
    fireeye.labnet:
    workstation.labnet:

ca:
  hosts:
    container.labnet:

hypervisor:
  hosts:
    workstation.labnet:

domains:
  hosts:
    media.labnet:

artifactstores:
  hosts:
    container.labnet:
    fireeye.labnet:

authservers:
  hosts:
    # container.labnet:

gitlabs:
  hosts:
    # workstation.labnet:

gpu_intel:
  hosts:
    container.labnet:

desktops:
  hosts:
    fireeye.labnet:
    workstation.labnet:

hires_desktops:
  hosts:
    workstation.labnet:

servicehosts:
  hosts:
    container.labnet:
    fireeye.labnet:
    workstation.labnet:

dbhosts:
  hosts:
    container.labnet:
    fireeye.labnet:
    workstation.labnet:

dnsresolvers:
  hosts:
    container.labnet:

mediaservers:
  hosts:
    fireeye.labnet:

metricsmonitors:
  hosts:
    fireeye.labnet:
    workstation.labnet:

kittens:
  hosts:
    workstation.labnet:
