all:
  hosts:
    canyon.labnet:
    container.labnet:
    laptop.labnet:
    storage-server.labnet:
    workstation.labnet:
    sandbox:
      ansible_connection: ssh
      ansible_host: fitting-toucan.sandbox.rm-rf.link
      ansible_user: admin

ca:
  hosts:
    workstation.labnet:

artifactstores:
  hosts:
    container.labnet:

authservers:
  hosts:
    container.labnet:

gitlabs:
  hosts:
    # workstation.labnet:

gpu_intel:
  hosts:
    canyon.labnet:
    container.labnet:

desktops:
  hosts:
    canyon.labnet:
    laptop.labnet:
    workstation.labnet:

hires_desktops:
  hosts:
    canyon.labnet:
    workstation.labnet:

servicehosts:
  hosts:
    canyon.labnet:
    container.labnet:
    storage-server.labnet:
    workstation.labnet:

dbhosts:
  hosts:
    canyon.labnet:
    container.labnet:
    storage-server.labnet:
    workstation.labnet:

dnsresolvers:
  hosts:
    container.labnet:

mediaservers:
  hosts:
    canyon.labnet:
    storage-server.labnet:

metricscollectors:
  hosts:
    container.labnet:

metricsmonitors:
  hosts:
    workstation.labnet:
