all:
  hosts:
    canyon.labnet:
    container.labnet:
    workstation.labnet:
    laptop.labnet:
    sandbox:
      ansible_connection: ssh
      ansible_host: fitting-toucan.sandbox.rm-rf.link
      ansible_user: admin

ca:
  hosts:
    workstation.labnet:

artifactstores:
  hosts:
    container.labnet:

authservers:
  hosts:
    container.labnet:

gpu-intel:
  hosts:
    canyon.labnet:
    container.labnet:

desktops:
  hosts:
    canyon.labnet:
    laptop.labnet:
    workstation.labnet:

hires-desktops:
  hosts:
    canyon.labnet:
    workstation.labnet:

servicehosts:
  hosts:
    canyon.labnet:
    container.labnet:
    workstation.labnet:

dbhosts:
  hosts:
    canyon.labnet:
    container.labnet:
    workstation.labnet:

dnsresolvers:
  hosts:
    canyon.labnet:
    container.labnet:

mediaservers:
  hosts:
    canyon.labnet:

metricscollectors:
  hosts:
    container.labnet:

metricsmonitors:
  hosts:
    workstation.labnet:
