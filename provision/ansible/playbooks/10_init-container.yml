---
- hosts: "{{ target_host | default('container') }}"
  gather_facts: true
  roles:
    - role: all
    - role: unique-container
    - role: gpu-intel
    - role: docker-jellyfin
    #- role: docker-nextcloud
    - role: docker-pihole
    - role: dyndns
