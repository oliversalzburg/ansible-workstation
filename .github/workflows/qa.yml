name: ansible-lint
on:
  pull_request:
  push:

jobs:
  build:
    name: Ansible Lint
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: |
          pipx install ansible ansible-dev-tools ansible-lint
          make requirements
      - name: Run ansible-lint
        run: |
          cd provision/ansible
          ansible-lint
